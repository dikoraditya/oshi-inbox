node_modules/
.next/
out/
.env
.env.local
*.tsbuildinfo
next-env.d.ts

# locally-stored media (MEDIA_STORAGE=local) — pics/videos, never commit
/public/media/

# deploy hand-off artifacts — data.sql holds private DM text; the tarball is ~GB.
# Generated by scripts/dump-data.ts + tar; never commit.
/db/data.sql
/media.tar.gz

# reverse-engineering captures — contain tokens + private messages, never commit
*.har
# raw API response dumps — same risk as .har, never commit
resp*.json

# The standalone collector tool is its own repo (oshi-inbox-collector). Anchored
# to the repo root so it doesn't also ignore src/app/api/collector/ (a route).
/collector/

# scratch/diagnostic scripts
_*.ts
_*.mjs
